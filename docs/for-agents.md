# For agents

!!! note "Placeholder"
    This page is owned by stream F2 (konyklabs/roadmap#74) and will be
    replaced with the consumer-side agent rules file — the longer counterpart
    to `vendorfake agent-setup`'s generated `.claude/rules/vendorfake.md`,
    with the recipes linked. F1 (this docs site) only wires the navigation
    entry so `uv run mkdocs build --strict` has something to link to before
    F2 lands; nothing below is durable content.

Until then: `vendorfake --help` and [the CLI reference](reference/cli.md)
describe every subcommand this distribution ships, and
[the API contract](api-contract.md) describes what is public.
