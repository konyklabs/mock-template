{
  "name": "full",
  "summary": "Every declared capability on. The default profile. Seeds one merchant so the OAuth dance works out of the box; the full scenario arrives in PR E, webhooks in PR D.",
  "capabilities": ["oauth", "orders", "payments", "inventory", "merchant", "customers", "chaos"],
  "seed": "seed/default.seed.json",
  "vendor": {
    "client_id": "UNITCLOVERAPP",
    "client_secret": "unit-clover-app-secret",
    "redirect_uri": "https://example.test/oauth/callback"
  },
  "chaos": { "seed": 20260829, "rules": [], "strict_rules": true },
  "clock": { "mode": "real" }
}
