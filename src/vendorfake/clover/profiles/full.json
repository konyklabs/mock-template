{
  "name": "full",
  "summary": "Every declared capability on. The default profile. Webhooks arrive in PR D, the seed scenario in PR E; until then the unit starts empty.",
  "capabilities": ["oauth", "orders", "inventory", "chaos"],
  "vendor": {
    "client_id": "UNITCLOVERAPP",
    "client_secret": "unit-clover-app-secret",
    "redirect_uri": "https://example.test/oauth/callback"
  },
  "chaos": { "seed": 20260829, "rules": [], "strict_rules": true },
  "clock": { "mode": "real" }
}
