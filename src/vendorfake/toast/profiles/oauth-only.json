{
  "name": "oauth-only",
  "summary": "Only the machine-client login (the name keeps the six-profile matrix shape every vendor shares; Toast's login is not OAuth). For a consumer testing token handling and nothing else.",
  "capabilities": [
    "auth",
    "chaos"
  ],
  "seed": "seed/default.seed.json",
  "vendor": {
    "client_id": "unit-toast-client-id",
    "client_secret": "unit-toast-client-secret"
  },
  "chaos": {
    "seed": 20260830,
    "strict_rules": true,
    "rules": []
  }
}
